org: greedyman
app: ocr
service: tesseract-ocr-example
frameworkVersion: ">=1.34.0 <2.0.0"

plugins:
  - serverless-python-requirements
  - serverless-pseudo-parameters
provider:
  name: aws
  runtime: python3.6 #issue- dont change version
  region: eu-central-1


functions:
  tesseract-ocr:
    handler: handler.main
    layers:
      - arn:aws:lambda:eu-central-1:105926622732:layer:tesseract:7
      # Update this ARN after you've deployed the tesseract layer
      # - arn:aws:lambda:#{AWS::Region}:#{AWS::AccountId}:layer:tesseract:1
    events:
      - http:
          path: ocr
          method: post